import { Schema } from "mwater-expressions";
/** Creates schema table for a custom tableset */
export declare class CustomTablesetSchemaBuilder {
    addTableset(schema: Schema, tableset: any): Schema;
}
